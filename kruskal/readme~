Wejście:

Wejście składa się z trzech linii. 
W pierwszej podajemy czasy wykonania poszczególnych zadań po spacji. 
W drugiej linii podajemy funkcję kosztu jako ciąg współczynników, tj. t^3-t+1 definiujemy jako [1,0,-1,1]. Ciągi współczynników oddzielamy spacją. Ważne, aby nie używać spacji w obrębie takiego ciągu (tj. [1,0,-1,1], a nie [1, 0, -1, 1]).
Trzecią linię stanowią ograniczenia kolejnościowe, każde ograniczenie oddzielone spacją. W obrębie jednego ograniczenia zadania oddzielone ciągiem znaków "->".


Wyjście:

Na wyjściu dostajemy informację o uszeregowaniu (indeksy zadań), wartość kryterium optymalizacji oraz (bardzo) prowizoryczny diagram. Wynikowy plik zostanie zapisany pod nazwą "output".


Dane wejściowe:

Znajdują się w plikach test1 i test2.


Uruchomienie:

python run.py [ścieżka do pliku]
np. python run.py test1


Uwaga:

Program nie jest idiotoodporny, tj. w przypadku nie podania ścieżki do pliku lub wpisaniu niepoprawnej liczby danych (czy to czasów wykonania, czy funkcji kosztu) wysypie się. Wyjątki nie są obsłużone - uznałem, że ta umiejętność nie będzie podlegała ocenie, a jedynie poprawne zaimplementowanie algorytmu. 
